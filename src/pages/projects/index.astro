---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const projects = (await getCollection('projects')).sort(
	(a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<Layout title="Projects">
	<section class="mx-auto max-w-4xl px-6 py-20">
		<h1 class="text-4xl font-bold tracking-tight mb-10">Projects</h1>

		<div class="grid gap-6 sm:grid-cols-2">
			{projects.map((project) => (
				<a
					href={`/projects/${project.id}`}
					class="block rounded-lg border border-slate-800 p-5 hover:border-slate-600 transition-colors"
				>
					<h2 class="font-medium text-lg mb-1">{project.data.title}</h2>
					<p class="text-sm text-slate-400 mb-3">{project.data.description}</p>
					<ul class="flex flex-wrap gap-2">
						{project.data.tags.map((tag) => (
							<li class="rounded-full bg-slate-800 px-2.5 py-0.5 text-xs text-slate-300">{tag}</li>
						))}
					</ul>
				</a>
			))}
		</div>
	</section>
</Layout>
